{
  "type": "postgres",
  "host": "localhost",
  "port": 54320,
  "username": "postgres",
  "password": "password",
  "database": "timery",
  "migrations": ["src/migration/*.ts"],
  "entities": ["src/**/*.entity.ts"],
  "cli": {
    "migrationsDir": "src/migration"
  }
}
